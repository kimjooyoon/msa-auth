version: '3.1'

services:
  mariadb:
    image: mariadb
    ports:
      - "3306:3306"
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: test
    volumes:
      - ./sql/:/docker-entrypoint-initdb.d/
